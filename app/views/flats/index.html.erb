<div class="conteiner p-3">
  <h1> All our Flats </h1>
  <div class= "d-flex flex-row">
    <% @flats.each do |flat| %>
    <div class= "d-flex card m-3 p-3 rounded">
      <p><%= flat.name %></p>
      <p><%= flat.description %></p>
      <div class ="d-flex flex row">
        <div class="d-flex flex row m-2">
          <p><%= flat.price_per_night %>€</p>
        </div>
          <p><%= flat.number_of_guests %></p>
      </div>
    </div>
    <% end %>
  </div>
</div>
